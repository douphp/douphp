<!--article_category.wxml-->
<navbar title="{{title}}"></navbar>
<view class="body">
 <view class="navbar" style="{{'top: ' + navigationBarAndStatusBarHeight}}">
  <view class="wrap">
   <scroll-view class="navScroll" scroll-x="true" scroll-left="{{scrollLeft}}" scroll-with-animation="true" style="{{'top: ' + navigationBarAndStatusBarHeight}}">
    <view class="item{{tree.cur == true ? ' active' : ''}}" wx:for="{{article_category}}" wx:key="index" wx:for-item="tree" data-catid="{{tree.id}}" data-index="{{index}}" bindtap="douMenu">{{tree.name}}</view>
   </scroll-view>
  </view>
 </view>
 <view class="navbar-space"></view>
 
 <view class="wrap">
  <view class="text-list">
   <navigator class="item" hover-class="mouseDown" wx:for="{{article_list}}" wx:key="index" wx:for-item="article" url="{{article.url}}">
    <view class="con{{article.image ? '' : ' no-img'}}">
     <view class="title">{{article.title}}</view>
     <view class="info">
      <text>{{lang.created_at}}：{{article.created_at}}</text>
      <text>{{lang.click}}：{{article.click}}</text>
     </view>
    </view>
    <view class="img" wx:if="{{article.image}}">
     <image src="{{article.image}}" mode="widthFix" />
    </view>
   </navigator>
   <view class="loadpage" wx:if="{{loadpage}}">{{lang.loadpage}}</view>
  </view>
 </view>
</view>